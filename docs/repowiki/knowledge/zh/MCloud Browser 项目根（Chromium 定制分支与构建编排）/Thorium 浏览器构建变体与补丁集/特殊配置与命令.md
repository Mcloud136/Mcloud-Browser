FFmpeg 独立构建使用 `python3 other/build_ffmpeg.py <android|linux|mac|win> [arch] -- [--ffmpeg_configure_flags]`，支持 `--branding Chrome|ChromeOS|Chromium`、`--config-only` 与 `--fast`（基于 `THIS_BUILD_WORKED` token 跳过重复构建）；Linux 安装后可通过 `other/thor_ver_linux/wrapper-*` 启动对应指令集版本的 Mcloud Browser。