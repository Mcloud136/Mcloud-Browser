构建入口为根目录的 `gn gen out/Default` + `ninja -C out/Default thorium`（或 `thorium_all`/`gn_all`）；Windows/Linux/macOS 安装包通过 `thorium_installer` 目标触发；Android 产物为 `chrome/android:chrome_public_apk`。MCloud 特有行为：在可执行文件同目录放置 `mcloud_flags.txt` 可在启动时追加命令行参数（--enable-features/--disable-features 会合并且用户参数优先级更高）。