在 Chromium 源码根目录执行 `./arm/setup_arm.sh` 将本模块的 build/media/third_party 复制进 `$HOME/chromium/src/...`；随后用 `gn args out/mcloud --args-file=arm/raspi/raspi_args.gn`（或 `win_ARM_args.gn`、`android/arm64_args.gn`）生成构建；Raspberry Pi 构建需 64 位 OS（Pi 3B+/4/400），ARM32 不被支持。