维护 `build-pipeline-flow.dot` 后需先用 Qoder 的 DOT Canvas 插件预览渲染结果，再更新 `build-pipeline-flow.evidence.md` 的行号引用；发布流程要求本地先构建出 `mini_installer.exe`，再通过推送 `v*` 标签触发 `.github/workflows/release.yml` 自动创建 GitHub Release。