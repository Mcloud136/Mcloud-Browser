所有脚本默认指向 `D:\wxmuma\chromium-src\src\out\mcloud\chrome.exe`，需通过 `-ChromeExe` / `-OutDir` 参数覆盖；`run_baseline.ps1` 一键输出 `docs/dev-logs/{版本}-benchmark.md`；`tools/check_features.py` 必须通过 `--src` 指定目标 Chromium 源码根目录，退出码 1 表示存在失效 feature。