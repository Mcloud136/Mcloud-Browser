构建命令：`cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build .`；可通过 `-DLGPL=OFF` 关闭 LGPL 模式；也可使用仓库内 `build.sh`、`test.sh` 一键构建与测试；Windows 用户可直接执行 `binaries/pak-win/pack.bat` / `unpack.bat` 或 `pak_mingw32.exe` / `pak_mingw64.exe`。