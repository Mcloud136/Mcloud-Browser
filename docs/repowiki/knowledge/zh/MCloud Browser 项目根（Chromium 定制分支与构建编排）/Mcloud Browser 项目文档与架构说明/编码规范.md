- 架构决策统一采用 `decisions/ADR-NNN-<主题>.md` 命名，并在文件头标注状态、日期、决策人与依据条款。
- 流水线相关文档成对出现：DOT 源图、Markdown 说明、证据清单与风险缺口文档共同构成单一架构视图。
- `superpowers/plans` 与 `superpowers/specs` 中的文件均以 `YYYY-MM-DD-<主题>` 日期前缀命名，便于按时间线检索。
- 开发日志与基准报告集中在 `dev-logs/`，按 Chromium 版本号（M150/M151）或阶段（phase1/2/3）组织，并提供 `benchmark-template.md` 作为统一模板。
- 根目录下的平台特定构建说明拆分为独立文件（`BUILDING_WIN.md`、`BUILDING_MAC.md`、`BUILDING_WIN_CROSS.md`），由 `BUILDING.md` 统一入口跳转。