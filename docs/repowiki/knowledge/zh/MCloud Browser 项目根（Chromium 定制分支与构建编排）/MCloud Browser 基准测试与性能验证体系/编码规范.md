- 每个 KPI 脚本以 `param(...)` 声明命令行参数并提供默认值，首行注释包含用法说明与方法依据（规范编号）。
- 通过创建 `$env:TEMP` 下的唯一临时用户数据目录并在使用后递归删除，保证每次测量在干净配置下执行。
- 多次采样结果统一通过本地 `Get-Median` 函数计算中位数作为最终指标输出。
- 脚本开头设置 `$ErrorActionPreference = "Stop"`，遇到错误直接终止而非继续执行后续步骤。
- 工具脚本遵循单一职责：一个文件对应一个 KPI 或一项维护任务，由 `run_baseline.ps1` 集中编排而非相互调用。